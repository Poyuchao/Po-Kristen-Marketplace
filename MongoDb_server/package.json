{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "homepage": "https://Poyuchao.github.io/Po-Kristen-Marketplace",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "concurrently \"nodemon server.js\" \"json-server --watch db.json --port 3000 --static .\"",
    "build": "react-scripts build",
    "heroku-postbuild": "npm run build"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^7.6.3"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "nodemon": "^3.0.1"
  }
}
